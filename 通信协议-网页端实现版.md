# Lindoway 通信协议 · 网页端实现版

> 本文件为网页端（APP）当前实现的协议格式，基于《Lindoway通信协议.md》整理，并对下行帧做了修订。
> 适用：MCU ↔ APP，BLE 透传（DX-BT36，DA14535，115200）。
> 约定：**所有多字节字段大端**，数值为缩放整数（无浮点）。

---

## 1. 通用帧格式

```
AA | Type | Payload（定长，按 Type 查表） | SUM | 55
```

- **SUM**：从 `AA` 到 Payload 末字节**累加**，取低 8 位。
- 无长度字段，帧总长由 Type 决定。
- 解析：字节流中找 `0xAA` → 读下一字节 Type 查帧长 N → 收满 N−2 字节 → 校验 SUM 正确且末字节 `0x55`，否则丢弃重找。

| Type | 方向 | 帧长 | 名称 | 状态 |
|---|---|---|---|---|
| 0x01 | MCU→APP | 26 | 姿态帧（50Hz） | ✅ 已确定 |
| 0x02 | MCU→APP | 10 | 事件帧（判姿报警） | ⚠️ 待定 |
| 0x80 | MCU→APP | 5 | ACK（回显动作类型） | ⚠️ 布局待定 |
| 0x82 | APP→MCU | 23 | 开始动作（目标姿态帧） | ✅ 已实现 |

> 下行（APP→MCU）当前**只有 `0x82` 一种帧**；动作结束由 MCU 自身静止检测判断，无需单独下发结束指令。

---

## 2. 下行（APP → MCU）

### 2.1 `0x82` 开始动作（目标姿态帧）—— 23 字节

下发目标 IMU 姿态，字段与 0x01 姿态帧**完全一致**（同缩放、同字节序），未使用字段填 0 表示"无目标"。

| 偏移 | 字段 | 类型 | 缩放 | 含义 |
|---|---|---|---|---|
| 0 | 帧头 | u8 | — | `AA` |
| 1 | Type | u8 | — | `82` |
| 2 | actionId | u8 | — | 动作编号（1=坐姿推举，2=站姿侧平举） |
| 3~4 | roll | i16 | ÷100 | 目标横滚角（°） |
| 5~6 | pitch | i16 | ÷100 | 目标俯仰角（°） |
| 7~8 | yaw | i16 | ÷100 | 目标偏航角（°） |
| 9~10 | ax | i16 | ÷1000 | 目标 X 加速度（g） |
| 11~12 | ay | i16 | ÷1000 | 目标 Y 加速度（g） |
| 13~14 | az | i16 | ÷1000 | 目标 Z 加速度（g） |
| 15~16 | gx | i16 | ÷10 | 目标 X 角速度（°/s） |
| 17~18 | gy | i16 | ÷10 | 目标 Y 角速度（°/s） |
| 19~20 | gz | i16 | ÷10 | 目标 Z 角速度（°/s） |
| 21 | SUM | u8 | — | 0~20 累加低 8 位 |
| 22 | 帧尾 | u8 | — | `55` |

**示例**（坐姿推举，目标 pitch=180°）：

```
AA 82 01 00 00 46 50 00 00 00 00 00 00 00 00 00 00 00 00 00 00 C3 55
```

> 还原：actionId=1；pitch 原始 0x4650=18000 → 180.00°；其余 0。SUM=0xC3。

---

## 3. 上行（MCU → APP）

### 3.1 `0x01` 姿态帧 —— 26 字节 @ 50Hz（✅ 已确定）

| 偏移 | 字段 | 类型 | 缩放 | 含义 |
|---|---|---|---|---|
| 0 | 帧头 | u8 | — | `AA` |
| 1 | Type | u8 | — | `01` |
| 2~5 | 时间戳 | u32 | 1 | 数据刷新时刻（ms）；**0=无有效数据** |
| 6~7 | roll | i16 | ÷100 | 横滚角（°） |
| 8~9 | pitch | i16 | ÷100 | 俯仰角（°） |
| 10~11 | yaw | i16 | ÷100 | 偏航角（°），0~360° 折叠到 ±180° |
| 12~13 | ax | i16 | ÷1000 | X 加速度（g） |
| 14~15 | ay | i16 | ÷1000 | Y 加速度（g） |
| 16~17 | az | i16 | ÷1000 | Z 加速度（g），静止 ≈ 1.000 |
| 18~19 | gx | i16 | ÷10 | X 角速度（°/s） |
| 20~21 | gy | i16 | ÷10 | Y 角速度（°/s） |
| 22~23 | gz | i16 | ÷10 | Z 角速度（°/s） |
| 24 | SUM | u8 | — | 0~23 累加低 8 位 |
| 25 | 帧尾 | u8 | — | `55` |

**示例**（roll 12.34°、pitch −5.10°、yaw 80.20°、az 1.000g、gy −2.0°/s）：

```
AA 01 00 00 30 39 04 D2 FE 02 1F 54 00 14 00 0A 03 E8 00 0F FF EC 00 03 63 55
```

### 3.2 `0x80` ACK —— 5 字节（⚠️ 布局待定）

```
AA | 80 | actionId(1，回显动作类型) | SUM | 55
```

> 收到 0x82 后回，回显动作编号，用于 APP 确认命令被正确装载。

### 3.3 `0x02` 事件帧 —— 10 字节（⚠️ 布局待定）

| 偏移 | 字段 | 类型 | 缩放 | 含义 |
|---|---|---|---|---|
| 0 | 帧头 | u8 | — | `AA` |
| 1 | Type | u8 | — | `02` |
| 2 | actionId | u8 | — | 动作编号 |
| 3 | flags | u8 | — | bit0 腕翻转 · bit1 行程不足 · bit2 借力甩动 |
| 4~5 | peakAngleDeg | u16 | ÷10 | 峰值角度（°） |
| 6~7 | durationMs | u16 | ÷100 | 动作耗时（×10 = ms） |
| 8 | SUM | u8 | — | — |
| 9 | 帧尾 | u8 | — | `55` |

---

## 4. 缩放汇总

| 量 | 单位 | 缩放 | 原始 → 实际 |
|---|---|---|---|
| 角度 roll/pitch/yaw | ° | ÷100 | 1234 → 12.34° |
| 加速度 ax/ay/az | g | ÷1000 | 1000 → 1.000g |
| 角速度 gx/gy/gz | °/s | ÷10 | 150 → 15.0°/s |
| 峰值角度（0x02） | ° | ÷10 | 900 → 90.0° |
| 耗时（0x02） | ms | ÷100 | 180 → 1.80s |

---

## 5. 动作编号与目标 IMU 值

| actionId | 动作 | 目标值（0x82 下发） |
|---|---|---|
| 1 | 坐姿推举 | pitch = 180°，其余 0 |
| 2 | 站姿侧平举 | roll = 90°，其余 0 |

> 注：具体动作对应 roll 还是 pitch，取决于表带佩戴方向，此处为暂定，待固件确认后调整。

---

## 6. 状态与待确认项

| 项 | 状态 |
|---|---|
| 通用帧格式 + `0x01` 姿态帧 | ✅ 已确定（沿用原协议文档） |
| `0x82`（IMU 口径目标姿态帧） | ✅ 已实现，下行唯一指令 |
| `0x80` ACK payload | ⚠️ 布局暂定（1 字节 actionId） |
| `0x02` 事件帧 payload | ⚠️ 暂定（标志位 + 峰值 + 耗时） |
| 动作 ↔ IMU 轴（roll/pitch）对应 | ⚠️ 待固件确认（佩戴方向） |
| notify 特征 UUID | ⚠️ 暂用 0xFFE1（写/通知同特征） |
