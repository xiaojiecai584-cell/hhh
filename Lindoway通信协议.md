# Lindoway 通信协议

MCU ↔ APP，BLE 透传（BT36，115200）。**所有多字节字段大端**，数值为缩放整数（无浮点）。

## 帧格式

```
AA | Type | Payload（定长，按 Type 查表） | SUM | 55
```

- SUM = 从 AA 到 Payload 末字节累加，取低 8 位
- 无长度字段，帧总长由 Type 决定

## 解析方式

```
1. 字节流中找 0xAA
2. 读下一字节 = Type，查表得帧总长 N
3. 收满 N−2 字节（Payload + SUM）
4. 校验：SUM 正确 且 最后一字节 = 0x55 → 采信，否则丢弃重找
```

## 0x01 姿态帧（26 字节，50Hz，MCU→APP）

| 偏移 | 字段 | 类型 | 缩放 | 含义 |
|------|------|------|------|------|
| 0 | 帧头 | u8 | — | 0xAA |
| 1 | Type | u8 | — | 0x01 |
| 2~5 | 时间戳 | u32 | 1 | 数据刷新时刻（MCU 内部 ms 计数）；**0 = 无有效数据** |
| 6~7 | roll | i16 | ÷100 | 横滚角（°） |
| 8~9 | pitch | i16 | ÷100 | 俯仰角（°） |
| 10~11 | yaw | i16 | ÷100 | 偏航角（°），0~360° 折叠到 ±180° |
| 12~13 | ax | i16 | ÷1000 | X 轴加速度（g） |
| 14~15 | ay | i16 | ÷1000 | Y 轴加速度（g） |
| 16~17 | az | i16 | ÷1000 | Z 轴加速度（g），静止 ≈ 1.000 |
| 18~19 | gx | i16 | ÷10 | X 轴角速度（°/s） |
| 20~21 | gy | i16 | ÷10 | Y 轴角速度（°/s） |
| 22~23 | gz | i16 | ÷10 | Z 轴角速度（°/s） |
| 24 | SUM | u8 | — | Byte0~23 累加低 8 位 |
| 25 | 帧尾 | u8 | — | 0x55 |

**还原**：实际值 = 原始值 ÷ 缩放。例：roll 原始 1234 → 12.34°；az 原始 1000 → 1.000g；gx 原始 150 → 15.0°/s。

**数据新鲜度**：时间戳 = MCU 刷新姿态数据的时刻；APP 用"收到时刻 − 时间戳"算数据年龄，正常 ≤ 15ms。传感器 200Hz → 100Hz 刷新 → 50Hz 上行，每帧都是发送瞬间的最新值。

**示例帧**（roll 12.34°、pitch −5.10°、yaw 80.20°、az 1.000g、gy −2.0°/s）：

```
AA 01 00 00 30 39 04 D2 FE 02 1F 54 00 14 00 0A 03 E8 00 0F FF EC 00 03 63 55
```

## 其他帧（payload 待定，随积木3/4 补全）

| Type | 方向 | 作用 |
|------|------|------|
| 0x02 | MCU→APP | 事件帧：判姿报警，插队发送（优先级高于 0x01） |
| 0x80 | MCU→APP | ACK：收到 0x82 后回，回显动作类型 |
| 0x82 | APP→MCU | 开始动作：动作类型 + 阈值参数块，MCU 收到即置零 + 装载 + ACK |
| 0x83 | APP→MCU | 结束动作：MCU 停止判姿，恢复待机 |
