# Lindoway 网页端 ↔ MCU 通信协议

> 版本：2026-09（含 ESP32-S3 / NUS 迁移 + 新增 0x83 停止采集帧）
> 本文以**网页端代码实际实现**为准（`src/core/protocol/`），供固件同学对照实现。

---

## 1. 物理连接

| 项 | 值 |
|----|-----|
| 硬件 | ESP32-S3（NimBLE 协议栈） |
| 蓝牙服务 | 标准 NUS（Nordic UART Service） |
| 设备广播名 | `Lindoway`（网页按此前缀过滤） |
| 采样率 | 50Hz（20ms 一帧，仅动作进行中上传） |

### GATT 结构

| 项 | UUID | 属性 | 方向 |
|----|------|------|------|
| 服务 | `6E400001-B5A3-F393-E0A9-E50E24DCCA9E` | — | — |
| RX | `6E400002-B5A3-F393-E0A9-E50E24DCCA9E` | Write | 网页 → 设备（下行） |
| TX | `6E400003-B5A3-F393-E0A9-E50E24DCCA9E` | Notify | 设备 → 网页（上行） |

> BLE 单包 ATT 载荷 20 字节，26 字节的 0x01 帧会被拆包，**浏览器 GATT 层自动重组**，网页无需处理分片。

---

## 2. 帧通用格式

```
AA | Type | Payload(定长) | SUM | 55
```

| 字段 | 长度 | 说明 |
|------|------|------|
| `AA` | 1 | 帧头 |
| `Type` | 1 | 帧类型 |
| `Payload` | 定长 | 见各帧定义；0x83 无载荷 |
| `SUM` | 1 | **从帧头到载荷末尾所有字节累加，取低 8 位**（即 `sum(byte[0 .. len-3]) & 0xFF`）|
| `55` | 1 | 帧尾 |

- 多字节字段一律**大端（big-endian）**；
- 有符号值用**二补码 i16**；
- 网页解析时校验 SUM 与帧尾，不符即丢弃该帧。

### 帧类型一览

| Type | 方向 | 名称 | 帧长 |
|------|------|------|------|
| `0x01` | 上行 | 姿态帧（50Hz） | 26 B |
| `0x02` | 上行 | 事件帧（provisional） | 10 B |
| `0x80` | 上行 | ACK（回显动作类型） | 5 B |
| `0x82` | 下行 | 开始动作（目标姿态） | 23 B |
| `0x83` | 下行 | **停止采集（新增）** | 4 B |

---

## 3. 上行帧（设备 → 网页）

### 3.1 `0x01` 姿态帧（26 字节，50Hz）

| 偏移 | 长度 | 内容 |
|-----:|-----:|------|
| 0 | 1 | `0xAA` 帧头 |
| 1 | 1 | `0x01` 类型 |
| 2 | 4 | u32 数据刷新时刻（ms） |
| 6 | 2 | i16 roll　×0.01° |
| 8 | 2 | i16 pitch ×0.01° |
| 10 | 2 | i16 yaw　×0.01° |
| 12 | 2 | i16 ax　×0.001 g |
| 14 | 2 | i16 ay　×0.001 g |
| 16 | 2 | i16 az　×0.001 g |
| 18 | 2 | i16 gx　×0.1 °/s |
| 20 | 2 | i16 gy　×0.1 °/s |
| 22 | 2 | i16 gz　×0.1 °/s |
| 24 | 1 | SUM（0~23 累加取低 8 位） |
| 25 | 1 | `0x55` 帧尾 |

**语义**
- 三个角度是**相对轨迹**：收到 `0x82` 时把当前姿态记为基准零点，之后上报 `当前姿态 − 基准`（±180° 折回）；
- 加速度/角速度为**原始值**；
- 设备未收到 `0x82` 前**不发任何上行数据**（待机静默）。

### 3.2 `0x02` 事件帧（10 字节，provisional，尚未实现）

| 偏移 | 长度 | 内容 |
|-----:|-----:|------|
| 0 | 1 | `0xAA` |
| 1 | 1 | `0x02` |
| 2 | 1 | actionId |
| 3 | 1 | flags：bit0 腕翻 / bit1 行程不足 / bit2 借力甩动 |
| 4 | 2 | u16 峰值角度 ×0.1° |
| 6 | 2 | u16 耗时 ×10 ms |
| 8 | 1 | SUM（0~7） |
| 9 | 1 | `0x55` |

> 当前判分在**网页端**基于 0x01 流完成，本帧暂未使用。

### 3.3 `0x80` ACK（5 字节）

| 偏移 | 长度 | 内容 |
|-----:|-----:|------|
| 0 | 1 | `0xAA` |
| 1 | 1 | `0x80` |
| 2 | 1 | actionId（回显） |
| 3 | 1 | SUM（0~2） |
| 4 | 1 | `0x55` |

> 当前版本尚未实现，网页端仅做兼容解析。

---

## 4. 下行帧（网页 → 设备）

### 4.1 `0x82` 开始动作（23 字节）

| 偏移 | 长度 | 内容 |
|-----:|-----:|------|
| 0 | 1 | `0xAA` 帧头 |
| 1 | 1 | `0x82` 类型 |
| 2 | 1 | actionId（1=坐姿推举，2=站姿侧平举） |
| 3 | 2 | i16 roll　×0.01° |
| 5 | 2 | i16 pitch ×0.01° |
| 7 | 2 | i16 yaw　×0.01° |
| 9 | 2 | i16 ax　×0.001 g |
| 11 | 2 | i16 ay　×0.001 g |
| 13 | 2 | i16 az　×0.001 g |
| 15 | 2 | i16 gx　×0.1 °/s |
| 17 | 2 | i16 gy　×0.1 °/s |
| 19 | 2 | i16 gz　×0.1 °/s |
| 21 | 1 | SUM（0~20 累加取低 8 位） |
| 22 | 1 | `0x55` 帧尾 |

**语义**
- 9 个 IMU 目标值与 `0x01` **同字段、同缩放、同大端**；
- 设备收到一条合法 `0x82` 后：记为基准零点 → 开始 50Hz 上行 → 翻转一次小灯（调试）；
- 网页端下发的角度由**正向运动学**（关节角 → 传感器姿态）算出，并与 3D 预览**共用同一套解算**（含俯卧的地面接触倾斜角）。

### 4.2 `0x83` 停止采集（4 字节）—— **本次新增**

| 偏移 | 长度 | 内容 |
|-----:|-----:|------|
| 0 | 1 | `0xAA` 帧头 |
| 1 | 1 | `0x83` 类型 |
| 2 | 1 | SUM = `(0xAA + 0x83) & 0xFF` = **`0x2D`** |
| 3 | 1 | `0x55` 帧尾 |

**实际字节：`AA 83 2D 55`**（无载荷）

**下发时机（由网页端决定）**
1. 用户在网页输入「本组目标次数」N；
2. 录制过程中网页按主轴波形**统计已完成次数**；
3. 达到 N 次 → 网页下发 `0x83`；
4. **手动点「停止录制」也会下发 `0x83`**（避免设备空转上行）。

**固件收到 `0x83` 后应**
1. **停止 `0x01` 上行**（回到待机静默，等下一次 `0x82`）；
2. 停止 `0x02` 事件帧；
3. 翻转一次小灯（调试）；
4. **保持 BLE 连接**，不要断开（下一组可直接再发 `0x82`）。

> 说明：为什么独立成帧而不是在 `0x82` 里加模式位？因为「开始」与「停止」是两个不同语义，混在一帧会让固件多一层状态判断，容易出错。

---

## 5. 交互时序

```
网页                                    设备
 │ 连接 GATT（NUS 服务）                    │
 │───────────────────────────────────────>│
 │ 发送 0x82（actionId + 目标姿态）          │
 │───────────────────────────────────────>│  记基准零点 / 小灯翻转
 │                                        │  开始 50Hz 上行
 │<─────────── 0x01 姿态帧（50Hz）─────────│
 │ （可选）0x80 ACK / 0x02 事件             │
 │<───────────────────────────────────────│
 │ 网页统计次数达标（或用户手动停止）          │
 │ 发送 0x83（停止采集）                     │
 │───────────────────────────────────────>│  停止上行 / 回到待机
```

**动作编号**

| actionId | 动作 |
|---------:|------|
| 1 | 坐姿推举 |
| 2 | 站姿侧平举 |

---

## 6. 变更记录

| 版本 | 变更 |
|------|------|
| 旧版（BT36）| STM32F407 + BT36 透传模块；厂商透传服务 `0xFFE0/0xFFE1`；逐字节回显 |
| 现版（ESP32-S3）| 改为标准 NUS（`6E400001/2/3`）；设备名 `Lindoway`；**协议帧格式不变**；不再回显 |
| 本次 | **新增 `0x83` 停止采集帧**（网页数够次数后下发） |

---

## 7. 待确认 / 已知近似

1. **设备广播名**：暂定 `Lindoway`，如改名需同步网页端过滤前缀；
2. **`0x02` 事件帧、`0x80` ACK**：尚未在固件实现，网页端已做兼容解析；
3. **`0x82` 里的加速度字段**：网页当前填 `0`，且不影响判分（判分只用主轴角速度 + 时长）；固件若用到请反馈；
4. **roll/pitch/yaw 与解剖轴的对应**：取决于实际佩戴方向，接真实硬件后需验证；
5. **判分依据**：`0x01` 上行流的主轴角速度 + 时长，配合 actionId 选阈值；`0x82` 不参与判分。
